import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    background: {
      default: "#23A384", // Change this to any color you want for the background
    },
  },
});

export default theme;
